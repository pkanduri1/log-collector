spring.application.name=log-bot
server.port=9090

# LangChain4j Configuration
# In-memory store is default if no other store is configured, but explicit is better if we had a specific one.
# For now, default auto-config works for In-Memory.
